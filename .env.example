# ===========================================
# VA App Environment Configuration
# ===========================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control

# -------------------------------------------
# REQUIRED: Google OAuth 2.0 Credentials
# -------------------------------------------
# 1. Go to https://console.cloud.google.com/
# 2. Create a new project or select existing
# 3. Go to "APIs & Services" > "Credentials"
# 4. Click "Create Credentials" > "OAuth client ID"
# 5. Choose "Web application"
# 6. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google
# 7. Enable APIs: Gmail API, Google Calendar API, Google Tasks API
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# -------------------------------------------
# REQUIRED: NextAuth.js Configuration
# -------------------------------------------
# Generate a secure secret: openssl rand -base64 32
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# Optional (recommended for public deployments): Restrict who can sign in
# Comma-separated exact emails
ALLOWED_EMAILS="you@example.com,other@example.com"
# Or allow an entire Google Workspace domain
ALLOWED_EMAIL_DOMAIN="example.com"

# -------------------------------------------
# OPTIONAL: AI Features
# -------------------------------------------
# OpenAI - For AI email drafting and planning assistant
# Get from: https://platform.openai.com/api-keys
OPENAI_API_KEY="sk-your-openai-api-key"

# Google Gemini - For AI email auditing (multi-model pipeline)
# Get from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY="your-gemini-api-key"

# -------------------------------------------
# OPTIONAL: Google Places API
# -------------------------------------------
# For location autocomplete in calendar events
# 1. Go to Google Cloud Console > APIs & Services > Library
# 2. Enable "Places API"
# 3. Create an API key with Places API restriction
GOOGLE_PLACES_API_KEY="your-google-places-api-key"

# -------------------------------------------
# OPTIONAL: Database (PostgreSQL)
# -------------------------------------------
# Only needed if you want persistent database storage
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user:password@localhost:5432/va_app"

# -------------------------------------------
# OPTIONAL: Web Push Notifications (VAPID)
# -------------------------------------------
# Generate keys: npx web-push generate-vapid-keys
# Subject should be mailto: or https:// URL identifying your server
NEXT_PUBLIC_VAPID_PUBLIC_KEY="your-vapid-public-key"
VAPID_PRIVATE_KEY="your-vapid-private-key"
VAPID_SUBJECT="mailto:your-email@example.com"
